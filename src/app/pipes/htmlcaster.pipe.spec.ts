import { HTMLCasterPipe } from './htmlcaster.pipe';

describe('HTMLCasterPipe', () => {
  it('create an instance', () => {
    const pipe = new HTMLCasterPipe();
    expect(pipe).toBeTruthy();
  });
});
